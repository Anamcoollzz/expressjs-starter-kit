<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= (typeof t==='function' ? t('app.title') : (title||'Admin')) %></title>

    <!-- Bootstrap CSS (single include) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
      body { background-color: #f8f9fa; }
      .container-main { padding-top: 1.5rem; padding-bottom: 2rem; }
    </style>
  </head>
  <body>
    <%- include('../partials/navbar') %>

    <main class="container container-main">
      <%- body %>
    </main>

    <!-- Bootstrap Bundle (single include) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- SweetAlert (single include + robust submit-only handler) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" defer></script>
  <script src="/vendor/sweetalert2.all.min.js" defer></script>
  <script src="/js/confirm.js" defer></script>
</body>
</html>
